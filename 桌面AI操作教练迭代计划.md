# 桌面 AI 操作教练迭代计划

## 1. 文档目标

本计划从空项目开始，按照“功能闭环”推进桌面 AI 操作教练，直到形成可安装、可试用、可持续迭代的 Windows 版本。当前 I0-I3 均未完成，I4 也尚未推进到多步教程、跨页面重新定位和无帧恢复切片。

拆分原则：

- 每个迭代都交付一个可运行的软件版本，而不是只交付底层模块。
- 以用户可感知的功能点拆分，相关的 UI、客户端、数据和测试放在同一迭代完成。
- 先形成实时桌面引导闭环，再扩展知识库和企业能力。
- 性能、隐私和状态恢复从第一版就纳入，不把它们留到最后补救。
- 迭代结束必须有明确的验收场景和可演示流程。

## 2. 最终交付定义

最终交付是一个 Windows 独立安装应用，具备以下完整能力：

```text
启动应用
  -> 顶部 PC 灵动岛常驻
  -> 本地滚动保留最近一段屏幕上下文
  -> 用户直接提问或放置箭头
  -> AI 理解过去操作和当前画面
  -> 在真实桌面上显示文字、箭头和虚拟鼠标动画
  -> 用户实际执行操作
  -> 系统验证完成情况并继续下一步
  -> 用户可保存为个人/团队知识流程
```

交付版本必须同时满足：

- 可安装、启动、更新和卸载；
- 默认不自动上传整段屏幕、不自动执行危险操作；
- 灵动岛不阻塞用户正常使用下层应用；
- AI 无法确定目标时会澄清，而不是播放错误指向；
- 断网、捕获暂停、模型失败和窗口变化有可恢复状态。

## 3. 固定技术边界

| 领域 | 首期选择 |
|---|---|
| 桌面客户端 | Python 3.14 + PySide6 + Qt Quick/QML |
| 业务核心 | Python，状态机、会话、协议、隐私策略和 AI 编排 |
| 屏幕采集 | 独立 Worker；首期 mss + Pillow/OpenCV，后期可替换原生 Worker |
| 本地数据 | SQLite/FTS5；屏幕帧使用应用私有固定磁盘槽位循环覆盖，内存只保留短小元数据；正常退出和下次启动清理临时帧 |
| AI 通讯 | HTTP + WebSocket/SSE，模型通过可替换网关接入 |
| 知识库 | 首期本地手册和流程，后期增加团队服务端知识库 |
| 打包 | PyInstaller + 安装程序 + 代码签名 |
| 首期行为边界 | AI 只做视觉指导，不自动点击、输入、删除或执行命令 |
| 首期平台 | Windows 10 22H2+/Windows 11 x64 |

任何需要 C++、Rust 或 C# 的性能模块都必须以独立 Worker 形式接入，不能把原生依赖直接耦合进 Python Core。

开发环境固定为仓库根目录 `.venv`；所有安装命令使用 `.venv\\Scripts\\python.exe`，依赖源默认使用清华 PyPI 镜像，禁止将项目依赖安装到系统全局 Python。AI 使用 OpenAI-compatible Provider，默认配置为：

```text
Base URL: https://opencode.ai/zen/v1
Chat Completions: https://opencode.ai/zen/v1/chat/completions
Models: https://opencode.ai/zen/v1/models
API Key: public
Default Model: deepseek-v4-flash-free
```

可选免费模型包括 `mimo-v2.5-free`、`north-mini-code-free`、`nemotron-3-ultra-free`、`deepseek-v4-flash-free` 和 `big-pickle`。I0 只实现 Provider 配置和 Mock 边界，I3 才接入真实请求。

## 4. 迭代总览

| 迭代 | 功能主题 | 版本结果 | 建议周期 |
|---|---|---|---:|
| I0 | 项目骨架与桌面外壳 | 能启动、退出、安装开发版的空壳应用 | 1 周 |
| I1 | PC 灵动岛与标注交互 | 能在任意桌面上稳定显示和编辑箭头 | 1-2 周 |
| I2 | 实时屏幕缓存与隐私 | 能本地滚动缓存、暂停和清除屏幕上下文 | 2 周 |
| I3 | AI 提问与首轮指导 | 能从时间窗口回答问题并播放一次指导 | 2-3 周 |
| I4 | 持续教程与步骤验证 | 能跟随用户操作、跨页面继续和纠错 | 2-3 周 |
| I5 | 应用知识库与成长流程 | 能导入手册、检索、审核并保存成功流程 | 2-3 周 |
| I6 | 稳定性、性能与安全 | 能在目标设备上稳定试用 | 2 周 |
| I7 | 安装交付、最终安全检测与 Beta | 通过发布前安全检测、可安装发布并可回滚 | 1-2 周 |

周期是开发估算，不是验收标准；每个迭代可根据模型服务、设备性能和测试反馈调整。

---

## 5. I0：项目骨架与桌面外壳

### 5.1 目标

建立可以持续扩展的 Python/Qt 项目基础，让应用能在真实 Windows 桌面上启动、关闭、恢复配置，并为灵动岛、采集 Worker 和 AI 服务预留明确边界。

### 5.2 功能范围

- 创建 Python 项目、依赖管理、配置文件和环境区分。
- 建立 `ui`、`core`、`capture`、`agent`、`knowledge`、`privacy`、`platform/windows` 目录边界。
- 创建 PySide6 应用入口、主进程和日志系统。
- 创建一个无业务内容的顶部透明窗口，支持置顶、拖动和退出。
- 创建本地 SQLite 数据库初始化与配置读写。
- 创建 Capture Worker 的空实现和版本化消息接口。
- 创建 AI Provider 的模拟实现，能返回固定结构化指导数据。
- 创建最基本的单元测试、启动脚本和开发说明。

### 5.3 可交付结果

```text
py -m app
  -> 显示一个可拖动的透明窗口
  -> 关闭后干净退出
  -> 再次启动时恢复上一次位置
  -> 日志写入本地脱敏文件
  -> 使用 Mock AI 返回一条测试指导
```

### 5.4 验收标准

- 在目标 Windows 设备上连续启动/退出 20 次无崩溃。
- 应用异常退出后，下次启动不会卡在半初始化状态。
- GUI 线程没有执行阻塞 I/O。
- Capture Worker、AI Provider 和 UI 之间没有互相导入实现细节。
- 能用一条命令启动开发版，能生成一个测试安装包或可运行目录。

### 5.5 不在本迭代做

不做真实屏幕捕获、AI 网络调用、知识文档解析和完整视觉风格；只把接口和最小空壳搭好。

---

## 6. I1：PC 灵动岛与标注交互

### 6.1 目标

让应用第一次具备可用的桌面形态：灵动岛好看、低干扰、可展开收起，并能在任意 PC 应用上放置、调整和清空箭头。

### 6.2 功能范围

- 夜航玻璃拟态：深墨蓝、无色玻璃、内高光、内暗边、月光蓝与香槟色。
- Apple Dynamic Island 级别的冷调毛玻璃：优先保证任何桌面背景上的一眼辨识与文字清晰，使用冷石墨半透明承托层、冰蓝窄边、顶部镜面高光、清晰外轮廓、内侧细边和低频周边流光；常驻岛不模糊桌面，受限 Acrylic 只用于隐私面板。禁止紫色渐变、纯黑或近似不透明黑色面板，以及使整个控件变糊的阴影效果。
- 参考图级别的文字可读性：白色文字使用局部对比度、细边界和文字阴影，彩色/浅色桌面背景上不能融入背景；字幕仍保持无卡片容器。
- 灵动岛收起态和展开态，顶部居中、拖动吸附和多显示器位置保存。
- 状态点、缓存占位、箭头、播放、历史和设置入口；灵动岛下方的输入坞作为独立交互层。
- 输入坞的空闲、聚焦、输入、提交中、失败和草稿保留状态。
- 历史按钮打开非模态小窗口；窗口失焦、选择会话或操作下层应用时不自动关闭。
- AI 字幕轨的空状态和测试消息样式：从灵动岛右侧进入、无卡片背景、带单条 `×`。
- 动态岛状态：空闲、采集占位、箭头模式、分析占位、指导占位、错误。
- 默认鼠标穿透；进入箭头放置和编辑时临时接管鼠标。
- 单击放置默认箭头，拖拽决定方向和长度。
- 拖动箭头主体移动，拖动首尾手柄调整起点和终点。
- 清空用户箭头、一次撤销、Esc 取消。
- 用户箭头与 AI 箭头分层和颜色区分。
- 全局快捷键初版：展开、聚焦输入、箭头模式、暂停占位、退出编辑。
- 系统托盘：展开、暂停占位、设置、退出。

### 6.3 可交付结果

一个不依赖真实 AI 的“桌面对话与标注版本”：用户可以打开任意软件，调出灵动岛，在下方输入坞输入测试问题，看到右侧字幕，打开不会自动关闭的历史窗口，放置箭头并继续正常操作下层应用。

### 6.4 验收标准

- 灵动岛在 100%、125%、150% DPI 和单/双屏上位置正确。
- 普通状态点击能穿透到下层应用；箭头主体和手柄能被正确命中。
- 放置箭头后自动恢复普通鼠标，不能把一次放置变成持续拦截。
- 清空只清除当前用户标注，不影响下层应用、会话和未来 AI 指导。
- 输入坞位于灵动岛下方，Enter 发送后保留问题历史，不因灵动岛收起而丢失草稿。
- 测试字幕从灵动岛右侧出现并持续显示；点击单条 `×` 只关闭该字幕。
- 历史窗口点击后保持打开，选择会话能显示详情，必须明确点击关闭才消失。
- 展开/收起动画不超过 600ms，不出现跳位、闪烁或文本溢出。
- 在彩色桌面背景测试页上，灵动岛能看到底层颜色和模糊光线；不能呈现成纯黑实心块。
- 在彩色和浅色背景上，标题、状态、按钮、输入占位文字和字幕均能清晰阅读；不能通过把整个面板变成不透明黑色来解决对比度。
- 灵动岛关闭后仍可通过托盘和快捷键恢复。

### 6.5 迭代结束演示

打开记事本、浏览器和 Unity 任意一个应用，分别完成“展开灵动岛 -> 放置箭头 -> 调整方向 -> 清空 -> 恢复正常操作”的演示。

---

## 7. I2：实时屏幕缓存与隐私控制

### 7.1 目标

让客户端在不需要用户手动截图的情况下，本地持续替换地保留最近 1 分钟屏幕上下文，并允许用户理解、暂停、清空和限制采集范围。

### 7.2 功能范围

- 独立 Capture Worker 与主 UI 进程通信。
- 当前显示器、当前活动窗口、指定显示器和应用白名单四种范围。
- 默认 1 分钟缓存，可设置 3/5/10 分钟；设置变化不影响现有会话协议。
- 低频 JPEG 压缩帧和基础画面变化检测；静态画面跳过编码与磁盘写入，仅保留低频心跳帧。关键帧提取和 WebM 分片后置到性能迭代。
- 固定磁盘槽位环形缓存：每个槽位被新帧直接覆盖，超过时长自动覆盖最旧帧，不进入历史，不在运行时逐帧创建/删除文件。
- 临时缓存设定 128 MB 物理上限；只有超过上限时才删除最旧槽位，常态不发生逐帧删写。
- 采集状态点、缓存时长显示、暂停和恢复。
- 用户可手动清空当前缓存；清空后不可从历史恢复。
- 正常退出时删除临时帧；下次启动先清除异常退出遗留帧；缓存目录保留为空目录，不留下帧或索引垃圾。
- 敏感应用黑名单、密码窗口规则和上传前脱敏占位。
- 锁屏、UAC/安全桌面、显示器断开和权限丢失时自动暂停。
- 缓存索引记录时间戳、显示器、活动窗口、画面变化和敏感标志，不记录键盘原文。

### 7.3 可交付结果

一个本地可运行的实时采集版本。此时还可以没有真实 AI，但能选择时间片段并生成测试上下文包。

### 7.4 验收标准

- 默认缓存最近 60 秒，超过时长自动覆盖。
- 采集 Worker 崩溃不拖垮灵动岛；主进程能显示采集异常并重启 Worker。
- 采集暂停后不再产生新帧，也不把旧帧伪装成当前画面。
- 清空后缓存索引和临时片段均不可恢复。
- 应用关闭后不保留临时帧；强制终止后的遗留帧会在下一次启动前清除。
- 1080p 设备上缓存、CPU、内存达到设计预算；GUI 线程保持响应。
- 指定黑名单窗口进入前台后，停止捕获或立即丢弃对应帧。

### 7.5 迭代结束演示

打开一个包含文字和弹窗变化的应用，等待 1 分钟，验证缓存可回看；切换到黑名单窗口和锁屏，验证缓存暂停；清空后确认无法生成上下文包。

### 7.6 I2 健康与清理闸门

- 静态画面跳过 JPEG 编码和槽位写入，只保留低频心跳帧；Worker 状态信号只在状态切换时发送。
- 常驻材质重绘最高 20 FPS，窗口隐藏时停止计时器；采集帧默认降采样到 1280 边长、JPEG 质量 55。
- 临时帧固定写入应用私有槽位，物理上限 128 MB；正常退出先停 Worker 再清空，下一次启动清理异常遗留帧。
- 配置原子写入只使用一个可识别临时文件，启动时回收；读取固定槽位时校验内容哈希，禁止串帧。
- UAC/Winlogon 等非 Default 输入桌面不采集；采集线程退出有等待上限，不能遗留后台 Worker。

---

## 8. I3：AI 提问与首轮指导

### 8.1 目标

打通产品最重要的第一条纵向闭环：用户直接提问，AI 获得过去时间窗口和当前画面，返回一条结构化指导，并在桌面上播放文字、箭头和虚拟鼠标。

### 8.2 功能范围

- 灵动岛下方输入坞：空闲、聚焦、输入、发送中、失败、草稿保留和取消。
- 锁定提问前时间窗口，提取关键帧、当前帧、OCR 摘要、窗口信息和用户箭头。
- 本地隐私过滤和上传前上下文预览。
- AI Provider：请求、流式响应、超时、重试、错误和 Mock 模式。
- 结构化 `GuideInstruction` 协议校验。
- 首轮 AI 回复按分析、判断、指令和完成条件拆成字幕，字幕从灵动岛右侧进入、持续保留并支持逐条关闭。
- 目标框、箭头、高亮和虚拟鼠标路径与字幕轨独立渲染。
- 灵动岛历史按钮打开不自动关闭的历史窗口；可选择会话并将消息重新显示到字幕轨。
- 置信度显示和低置信度澄清：圈选目标、提供按钮文字或重新提问。
- 播放、暂停、重播、关闭当前指导。
- 记录当前会话和 AI 响应摘要，不默认保存完整屏幕片段。

### 8.3 可交付结果

用户在记事本、Unity 或浏览器中输入问题，系统能根据测试画面返回一条真实可播放的指导。

### 8.4 验收标准

- 提问前的时间范围正确，不能意外上传缓存外的帧。
- AI 返回不符合协议时，客户端不崩溃、不执行未知动画，只显示可恢复错误。
- 高置信度目标能显示箭头和虚拟鼠标；低置信度不播放可能误导的点击动画。
- AI 动画只显示在覆盖层，不移动真实系统鼠标，不点击下层应用。
- 用户收起灵动岛后，当前指导状态保留；再次展开能继续查看。
- AI 字幕不会因为灵动岛收起、教程完成或鼠标离开而自动消失；关闭字幕不删除历史。
- 历史窗口失焦不自动关闭，显示历史消息不会复制出新的会话消息。
- 网络异常可重试，失败时可查看缓存状态或退出，不阻塞桌面操作。

### 8.5 迭代结束演示

预置一组脱敏截图和 Mock AI 响应，完整演示“提问 -> 时间窗口构建 -> AI 文本 -> 箭头 -> 虚拟鼠标 -> 重播/关闭”。再用真实模型完成一个只读操作指导。

### 8.6 当前实现状态（均未完成）

以下为计划内容，当前均未实现：

- 尚未接入 `GuideAgentService` 独立线程和 OpenAI-compatible `chat/completions` 请求；默认模型为 `deepseek-v4-flash-free`，超时和取消的恢复路径均未实现。
- 用户提问时只锁定当前时间窗口，最多均匀抽取 6 帧；单帧超过 1.5 MB 或槽位哈希不匹配时跳过，不伪造旧画面。
- AI 输出必须是结构化 `GuideInstruction`；Markdown JSON 可兼容解析，纯文本只以低置信度澄清显示，非法动作不会执行。
- 视觉坐标使用归一化坐标，覆盖层会限制在虚拟桌面范围内；箭头、高亮和虚拟光标保持鼠标穿透，不移动真实鼠标。
- I3 尚未推送提交（计划提交信息：`feat: implement I3 first-turn guide agent`）。

---

## 9. I4：持续教程与步骤验证

### 9.1 目标

从“一次回答”升级为“持续操作教练”：用户实际完成一步后，系统重新观察画面、判断结果，并进入下一步或给出纠正。

### 9.2 功能范围

- 教程步骤状态机：计划、展示、等待用户、验证、成功、需帮助、风险阻塞、跳过和完成。
- 每一步包含目标、动作说明、动画数据、完成条件、失败分支和下一步捕获要求。
- 后续画面采集、OCR/视觉变化判断和完成验证。
- “我完成了”“不对”“重播”“跳过”“重新定位”操作。
- 页面切换检测：旧目标失效时停止动画并等待新页面。
- 用户圈选目标和候选目标确认。
- 用户反馈记录：完成、未完成、不确定、跳过和文字备注。
- 会话恢复：应用重启后可恢复最近一次未完成教程的摘要，不恢复敏感原始帧。
- 基础风险提示：修改配置、删除、管理员操作前必须进入确认状态。

### 9.3 可交付结果

一个至少包含 3-5 个步骤的跨页面教程，可以在用户自己操作后推进下一步，并在定位失败时要求澄清。

### 9.4 验收标准

- 每一步都有明确的完成或不确定结果，不能无限等待。
- 用户页面未切换时，不显示下一页的旧坐标箭头。
- 用户操作失败后，系统会指出可能原因或提供重试路径。
- 连续 5 步教程中，重播、暂停、收起、重新展开和错误恢复不丢失状态。
- 高风险动作不会被普通“完成”按钮跳过确认。
- 完成教程后，状态能正常淡出，历史中保留步骤摘要和结果。

### 9.5 迭代结束演示

完成一个跨页面流程，例如“打开 Windows 存储设置并查看临时文件”或“Unity 打开 Console、定位错误、返回 Inspector”，中途故意点错一次并验证纠错，再切换页面验证继续指导。

### 9.6 待开发切片与开发重启约定

- `app/core/tutorial_state.py` 尚未交付第一切片：教程步骤和状态机不依赖 Qt，显式覆盖计划、展示、等待用户、验证、纠错、暂停、重播、跳过、风险阻塞、取消和完成。
- 状态机只保留步骤摘要与最近 32 条状态事件，不能把原始屏幕帧或键盘原文写入恢复数据；验证结果必须由用户确认或后续只读视觉验证器提供。
- 开发重启脚本保留为显式手动工具，但本轮及后续默认不启动、不常驻监控；需要重启时由开发者手动关闭并重新运行应用，避免后台 watcher 对用户电脑造成干扰。
- 生产更新和回滚仍放在 I7；任何更新器都必须有边界、签名、回滚和退出清理审计。

### 9.7 当前实现状态（均未完成）

以下为计划内容，当前均未实现：

- `GuideInstruction` 可携带最多 8 个受校验的有序步骤；每一步复用动作、风险、坐标和字段大小限制，模型输出未知字段或越界坐标会在覆盖层之前被拒绝。
- “我已完成”只读取最近约 12 秒的已有缓存帧，发起带完成条件的验证请求；验证成功后先隐藏旧页引导，再重新分析最新画面并定位当前的下一步，不能把旧页面坐标带到新页面。
- 教程摘要保存到本地 SQLite：只含步骤标题、文字说明、完成条件、风险、页面签名和状态索引，不含原始帧、目标坐标或窗口内容；启动恢复后必须重新定位，不能恢复旧视觉引导。
- 灵动岛“教程”菜单提供重播、我已完成、需要帮助、暂停/恢复、跳过、确认风险步骤和关闭；跨页面多步编排、无帧恢复和风险确认尚未接入，OCR/视觉验证器仍留在后续切片。
- **轻量视觉完成证据**：每个可见步骤仅在进程内保留一条滚动缓存帧的元数据作为基线；点击“我已完成”时读取已有基线和最新帧，缩小到 `96x54` 灰度图后计算平均差异，随即释放解码像素。非 `manual` 完成条件在“证据可用且无明显变化”时，不能接受模型的 `completed=true`，而是转为不确定并进入纠错；基线槽位被覆盖、帧损坏或缓存不足时只降级为“证据不可用”，不会误判失败。该证据不写入 SQLite，不新增截屏、持续内存或磁盘文件。

### 9.8 指导动画设计

- **术语与模式**：采用标准 Onboarding coach mark、Tutorial overlay animation 和 Cursor animation 术语。视觉结构参考 Minecraft Create/Ponder：点击手势是主叙事角色，连接线表示操作路径，编号节点表示步骤顺序，箭头头部表示下一方向。
- **节奏**：先显示手和目标连线，再沿曲线依次点亮 1、2、3 步骤节点，方向箭头顺着连接线推进，最后手停在目标处并播放一次点击波纹。用户先看到“从哪里开始”，再看到“按什么顺序走”，最后看到“目标在哪里”。
- **视觉层级**：连接线使用亮青色主线、暗色描边和一条低亮度点状副线；步骤节点使用橙色圆形徽章与白色矢量数字；手势采用本地捆绑的 Noto Emoji `1F446` SVG（OFL-1.1），客户端只做位移、旋转、发光、按压缩放和点击爆发；目标只保留清晰焦点轮廓，不铺满桌面的遮罩。
- **资源边界**：不直接打包 Unity Asset Store 的 `Gameplay Guiding Hand With Effects` 等不可确认再分发的商业包；第三方手势文件固定到 Noto Emoji commit `8998f5dd683424a73e2314a8c1f1e359c19e8742`，保留原始授权文本和来源，方便后续替换为用户自有或明确 CC0/商业授权资源。
- **时间轴**：默认约 3.1 秒，包含 180ms 引导出现、720ms 连接/方向生长、最多 1.2 秒手势路径和 1.2 秒目标停留；不循环、不自动消失，完成后保留最终指向。
- **生命周期**：灵动岛输入坞直接提供重播和关闭图标。关闭属于 hide，不删除最近指导；重播从时间轴零点重建动画，并将已取消/已完成步骤重新置为等待用户。彻底清空或跳过步骤才删除指导快照；用户自己的橙色箭头不受影响。
- **性能与安全**：播放期间约 30 FPS，动画完成或暂停后停止计时器；路径最多 64 个点，坐标受虚拟桌面边界限制；覆盖层始终鼠标穿透，不移动真实鼠标、不执行点击或输入。

---

### 9.9 引导编排、定位与视觉规则（尚未进入 I4）

#### 元素选择

| 情况 | 显示元素 | 不显示元素 |
| --- | --- | --- |
| 单点点击、双击或右键 | 目标框、点击手势、局部点击波纹 | 路线、序号 |
| 拖拽 | 起点点击手势、细橙色路线、终点目标框 | 多余箭头 |
| 输入文字 | 输入框目标框与字幕指令 | 手势、路线、序号 |
| 滚动、方向选择、目标在当前页不可见 | 细橙色方向箭头与字幕 | 手势、虚构目标框 |
| 同一页面内 2-3 个不可拆分的连续子动作 | 细橙色路线和明确的 1-3 号节点 | 第 4 个及之后节点 |
| 跨页面教程 | 当前页只显示一个动作；完成后重新抓取并重新定位 | 复用上一页路线、坐标或序号 |

路线一律使用细橙色虚线和小型橙色箭头。蓝色只属于目标框、焦点环和状态提示，不能作为路线。步骤节点不是装饰：只有模型明确返回 `step_markers` 且节点数量不超过 3 时才绘制。每个教程的全局步骤序号由灵动岛状态机管理，不在桌面上同时铺满。

#### 精准定位链路

1. 捕获后为每一帧记录原始采集区域 `(left, top, width, height)`；它可以是活动显示器、指定显示器或活动窗口，允许负坐标多显示器布局。
2. 模型只根据锁定帧返回 `0..1` 的归一化目标框、箭头端点、路线点和步骤节点；协议拒绝超范围、非有限值、超过 64 个路线点或超过 3 个节点的输出。
3. 覆盖层将坐标映射回该帧的原始采集区域，并与虚拟桌面求交集；绝不按整个桌面盲目放大。这样活动窗口采集、缩放截图和副屏都能保持同一目标位置。
4. 客户端再执行最小尺寸、屏幕边界和鼠标穿透处理；无法定位、目标框过小或当前帧已变化时不绘制猜测坐标，而是显示字幕并请求用户切页或重新定位。
5. 用户完成一步后，旧覆盖层立即隐藏；验证通过才锁定最新帧并请求下一步。重启恢复也只恢复文字摘要，必须重新定位，不能恢复旧坐标。

#### AI 编排约束

- `click`、`double_click`、`right_click`：只返回目标框和最终点击点；客户端显示点击手势，不展示路线。
- `drag`：才允许 `show_route=true`，并可返回 2-8 个路线点；客户端播放手势沿路线移动，路线完成后保留终点。
- `type`：只高亮输入框和显示文字指令，避免手势造成“软件会代替输入”的误解。
- `arrow`：仅用于滚动方向、展开方向或当前页面没有可靠点击目标的情况。
- `step_markers`：只用于同屏的 2-3 个连续子动作；跨页步骤不能标号，也不能伪造下一页目标。
- 模型输出永远只是视觉说明。客户端不会依据任何视觉字段调用鼠标、键盘、窗口控制、文件或系统命令。

#### 资产与动画

点击手势采用用户授权的本地素材 `guidance_tap_hand.png`，包含手掌与点击波纹；应用仅改变位置、透明度、缩放和按压节奏，不联网下载素材。路线动画使用有界时间轴，完成后停止计时器并保留最终画面；关闭、跳过、跨页或退出时释放覆盖层状态。所有临时屏幕帧仍由固定槽位缓存管理，手势资产不写入用户数据目录。

#### 验收场景

- 在副显示器的 Unity 窗口中请求“拖拽材质到对象”，路线和手势均在副屏窗口区域内，不偏移到主屏。
- 请求“点击保存”，只显示目标框和点击手势，不出现路线或序号。
- 请求“展开菜单后再选择项”，当前页只显示第一步；用户展开后，系统重新抓取当前画面再定位菜单项。
- 模型返回 4 个节点、越界坐标或错误类型时，安全校验拒绝输出，覆盖层不绘制任何元素。

## 10. I5：应用知识库与成长流程

### 10.1 目标

让 AI 能指导训练集里没有的企业/个人应用，并把用户确认成功的操作沉淀为可检索、可版本管理、可追溯的多模态证据图谱。I5 不调用专用 embedding API 或安装专用 embedding 模型，但允许普通免费 Chat/多模态模型按系统规则生成语义向量，并通过本地向量表/可替换向量服务参与混合检索。

### 10.2 功能范围

- 混合 N-MM/A-MM 证据图谱：文档版本、章节、文本块、表格、图片、OCR 区域、视频片段、任务、步骤、Claim 和 EvidenceAnchor 可作为节点和关系表达。
- 应用私有内容寻址对象库：原始对象按 SHA-256 只写一次，SQLite 只保存引用、属性和图关系，不使用 BLOB。
- 四层导入治理：Quarantine、Sandbox Parse、Untrusted Graph、Reviewed Graph；导入记录、拒绝原因、审核人和版本账本可追踪。
- 第一切片支持受限 Markdown/TXT/HTML；PDF/Word/视频/OCR 只保留 schema/适配器边界，不在本迭代伪装成已实现。
- SQLite/FTS5 词法召回 + 应用/版本/语言/信任状态过滤 + 图谱邻接规则扩展；可叠加普通 Chat 模型生成的固定 64 维语义向量做余弦相似度，不调用专用 embedding 模型。
- 普通免费 OpenAI-compatible Chat/多模态模型既可按严格 JSON 协议生成语义向量，也负责证据包上的理解、排序和指导生成；所有输出必须校验并带证据引用，不能执行文档命令或写回知识库。
- 草稿、审核、发布、过期和归档状态；用户成功流程只有在脱敏、去重和明确确认后才能进入个人草稿。
- 所有结果引用原始对象哈希、文档版本、页/区域/时间点或文本偏移；删除索引不删除用户原始外部源文件。
- quarantine、解析缓存、缩略图和 OCR 派生物使用 TTL/LRU/引用计数清理；不频繁写盘、不逐帧落盘、不产生长期垃圾。

### 10.3 可交付结果

导入一份内部应用 SOP 后，系统能生成带来源锚点的待审核图谱；审核后通过 FTS 和图谱上下文检索给普通免费模型，AI 能返回带引用的任务步骤。用户成功完成一次流程后，可以在脱敏预览通过后保存为个人草稿。

### 10.4 验收标准

- 当前应用和版本优先于通用知识；版本冲突时明确提示。
- 未审核或隔离内容不会进入默认高置信度指导；外部文档中的命令不会被执行。
- 每条被 Agent 使用的 Claim 都能沿 `步骤 -> Claim -> EvidenceAnchor -> 版本/定位 -> SHA-256` 追溯。
- 用户保存流程前可以检查时间段、截图和脱敏结果。
- 删除知识后，搜索和图谱引用不再返回该内容；无引用对象和过期临时对象按清理策略回收，但不删除用户原始外部文件。
- 旧版本流程不会静默覆盖新版本流程。
- 不安装或调用专用 embedding 模型；语义向量必须记录模型 ID、规则版本、维度和校验结果，且不能绕过 FTS、图谱邻接、审核状态和引用链。
- 导入失败、资源超限、压缩炸弹、脚本内容和重复对象都有可审计结果。

### 10.5 迭代结束演示

导入一个虚构 ERP 的 Markdown/HTML SOP，检查隔离记录和对象哈希；审核“新增供应商”流程；使用 FTS + 图谱相邻上下文并按规则请求免费 Chat 模型生成语义向量，再构建混合证据包；再次提问时验证 AI 优先检索该流程并显示版本/定位引用。随后删除知识并执行清理，确认索引、图谱、向量和无引用对象消失而外部原文件仍在。

### 10.6 当前实现状态（均未完成）

以下为计划内容，当前均未实现：

- 尚未实现：应用私有内容寻址对象库、来源版本账本、Quarantine 导入记录、证据图谱节点/边/锚点、受限 Markdown/TXT/HTML 解析和带引用的 FTS 检索。
- 尚未实现：保持 `KnowledgeStore` 既有文档/流程接口，新增审核信任状态、证据详情、语义向量账本、向量相似度/混合检索、语言过滤和有界清理。
- 尚未实现：`ChatModelVectorizer` 通过普通 OpenAI-compatible Chat 模型生成固定维度 JSON 语义指纹，严格校验响应；不安装或调用专用 embedding 运行时，向量化由显式任务触发且失败自动保留 FTS 兜底。
- 后续适配器：PDF/Word/视频/OCR、多模态媒体解析、批量向量重建和更完整的 Agent 证据包编排；不把任意解析器或外部命令放入主进程。

---

## 11. I6：稳定性、性能与安全

### 11.1 目标

把功能原型变成可以交给真实用户试用的稳定版本，重点处理持续运行、隐私、资源占用、权限和异常恢复。

### 11.2 功能范围

- 采集 Worker watchdog、自动重启和异常状态展示。
- 多显示器、DPI、窗口移动、显示器拔出和活动窗口切换的稳定处理。
- 1/3/5/10 分钟缓存的资源测试和自适应降采样。
- 低性能模式：关闭颗粒、降低模糊、降低采样和动画帧率。
- 敏感窗口、密码输入框、UAC、锁屏、远程桌面和全屏应用测试。
- 本地缓存加密/覆盖、清空验证、日志脱敏和上传前检查。
- 模型限流、失败重试、请求取消和网络断开恢复。
- 崩溃日志、性能指标、AI 延迟和步骤成功率统计。
- 键盘可用性、高对比度、减少动效和中文文案审查。

### 11.3 可交付结果

一份可重复安装的 Release Candidate，能连续运行一整天，具备隐私默认值和明确的故障恢复入口。

### 11.4 验收标准

- 空闲 CPU、采集中 CPU、内存、缓存空间和动画 FPS 达到设计预算。
- 运行 8 小时不会出现缓存无限增长、窗口泄漏或 UI 越来越卡。
- 采集、AI、知识库任一服务异常时，其他部分仍能安全退出或继续使用。
- 密码、UAC 和黑名单窗口内容不进入云端上下文。
- 高风险系统指导显示警告、备份建议和逐项确认。
- 关键用户路径在高 DPI、多屏和网络中断下可恢复。

### 11.5 迭代结束演示

在低配和常规设备各运行 8 小时，执行多屏、暂停、锁屏、网络断开、Worker 崩溃和模型超时注入测试，输出性能和异常报告。

### 11.6 当前安全切片（均未完成）

以下为计划内容，当前均未实现：

- `app/security/audit.py` 统一检查缓存路径是否越界、临时帧是否清理、Provider 是否使用允许的 HTTPS 域名，以及 AI 指令动作、风险、字段大小和归一化坐标。
- `parse_guide_content` 在视觉覆盖层之前调用同一校验器；异常模型输出只转为可恢复错误，不进入动画或动作执行路径。
- `python -m app.security` 尚未提供离线审计入口；I7 的严格脚本还必须通过 `pip-audit`、依赖许可证、安装包完整性和更新回滚检查。

---

## 12. I7：安装交付、最终安全检测与 Beta 发布

### 12.1 目标

将稳定版本变成真实用户可以安装、更新、卸载和反馈的 Windows Beta 产品。

### 12.2 功能范围

- PyInstaller 依赖收集、Qt 资源、采集 Worker 和模型配置打包。
- x64 安装程序、卸载程序、版本号和安装目录策略。
- 开机启动选项、系统托盘、快捷方式和默认隐私配置。
- Windows 代码签名、安装包完整性和更新通道。
- 增量/全量更新、更新失败回滚和旧版本兼容。
- 首次运行向导、隐私政策、用户协议和错误反馈入口。
- Beta 配置：默认禁用高风险实验功能，允许用户导出脱敏诊断包。
- 发布前最终安全检测机制：敏感窗口/UAC/锁屏拦截回归、缓存清理和路径越界验证、高风险命令/删除/管理员操作阻断、AI 输出协议和坐标边界模糊测试、依赖漏洞扫描、权限与网络上传审计、日志脱敏审计、长时间运行资源泄漏检测、安装包签名/完整性/更新回滚验证。
- 发布说明、已知问题、回滚办法和数据清除说明。

### 12.3 可交付结果

```text
桌面AI操作教练-0.1.0-beta.exe
  -> 安装
  -> 首次启动配置隐私和缓存
  -> 完成一个指导任务
  -> 更新到 0.1.1
  -> 保留个人设置和知识库
  -> 可安全卸载并清除临时缓存
```

### 12.4 验收标准

- 在干净 Windows 用户账户上安装、启动、退出和卸载成功。
- 安装包不要求用户安装 Python、Node、C# 或开发工具。
- 更新失败时能回到可用版本，不丢失用户知识库和配置。
- 安装、权限、联网、采集、上传和清除行为都有用户可读提示。
- Beta 用户反馈可以关联版本、应用、步骤和脱敏诊断信息。
- 最终安全检测报告可复现：每项检测有输入、预期阻断/放行结果、日志证据和修复归属；未通过安全项不能发布。

### 12.5 迭代结束演示

从干净虚拟机安装 Beta，完成首次向导和一个跨页面教程，升级一次、导入一个手册、导出脱敏诊断包，再卸载并验证临时缓存清除。

### 12.6 最终安全检测清单

| 检测域 | 必须验证的结果 |
|---|---|
| 屏幕与隐私 | 密码窗口、UAC/Winlogon、锁屏、远程桌面和黑名单窗口不进入 AI 上下文；暂停后无新帧写入 |
| 文件与清理 | 所有缓存路径解析后仍在应用目录内；正常退出、异常重启和卸载后不残留帧、临时文件或后台 Worker |
| AI 协议 | 非法 JSON、未知动作、超长字段、越界坐标、提示注入和恶意 URL 只产生可恢复错误，不执行动作 |
| 权限与网络 | 无管理员权限运行；上传只发生在用户提交问题后；请求域名、大小、超时和重试有上限，日志不含密钥和原始画面 |
| 资源与稳定性 | 8 小时运行无缓存、内存、线程、句柄和 GPU 重绘持续增长；采集/AI 任一故障都能退出或恢复 |
| 供应链与更新 | 依赖漏洞检查、安装包签名和哈希校验通过；更新失败自动回滚且不丢失知识库和配置 |

### 12.7 可执行检查入口

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\security_audit.ps1
```

脚本按编译、单测、应用审计、依赖漏洞四步执行；缺少 `pip-audit` 时严格模式失败，只有显式 `-AllowMissingDependencyAudit` 才允许开发环境继续。

---

## 13. 迭代之间的质量闸门

每次迭代结束都必须通过以下检查，才能进入下一迭代：

### 功能闸门

- 本迭代的用户路径可以从干净状态开始并完整结束。
- 取消、重试、关闭、重启和错误状态都有明确结果。
- 新功能不破坏上一迭代已经验收的核心流程。

### 工程闸门

- 单元测试、协议校验和关键集成测试通过。
- 日志能定位失败阶段，但不包含未经授权的原始屏幕内容。
- GUI 主线程无长时间阻塞操作。
- 配置、数据 schema 和协议有版本号或迁移策略。

### 体验闸门

- 灵动岛的状态、文案、颜色和动效仍符合设计令牌。
- 低置信度、隐私暂停和错误状态能被普通用户理解。
- 操作不遮挡下层应用，不误触，不移动真实鼠标。

### 安全闸门

- 本迭代新增的数据都有默认保留期限和清除路径。
- 高风险动作没有绕过确认。
- 知识共享和诊断上传不会默认扩大数据范围。

---

## 14. 变更控制

开发中如果出现新需求，先判断它属于哪一种：

| 类型 | 处理方式 |
|---|---|
| 修复当前迭代验收失败 | 直接修复，不新增范围 |
| 不影响核心协议和状态机的小体验优化 | 加入当前迭代的余量或下一迭代 |
| 改变采集、AI 输出、知识结构或窗口行为 | 记录架构决策，评估后进入新迭代 |
| 引入真实自动化执行、跨平台或企业权限 | 新建独立版本目标，不塞入 MVP |

每个重大变更记录：背景、用户价值、影响模块、数据兼容策略、性能/隐私影响和新的验收标准。

## 15. 开工顺序

第一周直接完成 I0 的项目骨架和 I1 的最小灵动岛垂直切片；不要先做完整知识库、完整 AI Agent 或完整设置页。

推荐第一条可运行路径：

```text
启动
  -> 灵动岛收起/展开
  -> 进入箭头模式
  -> 放置并编辑箭头
  -> 清空并恢复鼠标穿透
  -> 退出并再次启动恢复位置
```

这条路径稳定后，再把 I2 的实时采集接到同一个灵动岛状态体系中，随后接入 I3 的 Mock AI，最后替换为真实模型。这样任何阶段都能演示真实产品，而不是等全部后端完成后才知道桌面交互是否可用。
